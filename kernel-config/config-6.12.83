# WWAN (无线广域网) 和 MHI (调制解调器主机接口) 核心支持
CONFIG_NETDEVICES=y
CONFIG_PCI=y
CONFIG_WWAN=y
CONFIG_MHI_BUS=y

# MTK T700 系列 5G 模块驱动 (这是你的FM350-GL模块)
CONFIG_MTK_T7XX=y

# WWAN 子设备驱动 (用于创建 /dev/wwan* 接口)
CONFIG_MHI_WWAN_CTRL=y
CONFIG_MHI_WWAN_MBIM=y
